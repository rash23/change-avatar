// Polling interval for background generation status updates
// 1000ms (1 second) is optimal for mock functionality
export const POLLING_INTERVAL = 1000

// Background generation duration in milliseconds
export const GENERATION_DURATION = 5000

export const DEFAULT_IDEA_PROMPT =
  'Animate glowing rays pulsating from behind the bottle, leaves gently swaying, and golden sparkles floating upward for a natural, radiant effect.'

export const GENERATION_BUTTON_TEXT = {
  idle: 'Generate BG for 1 credit',
  generating: 'Generating...',
}

export const IDEA_MAX_LENGTH = 200
